import { SelectAndClickDirective } from './select-and-click.directive';

describe('SelectAndClickDirective', () => {
  it('should create an instance', () => {
    //const directive = new SelectAndClickDirective();
    //expect(directive).toBeTruthy();
  });
});
